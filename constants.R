# UI Styles
STYLES <- list(
  ACTIVE = "color:black;",
  INACTIVE = "color:gray;",
  DANGER = "background-color: #d9534f; color: white;"
)

# File Paths
PATHS <- list(
  SCORES = "data/score.csv",
  QUESTIONS = "data/qlist.csv"
)

# Default Values
DEFAULTS <- list(
  USER= "guest",
  ZERO_INCLUSION = 5L,
  BIAS_VALUE = 1.6,
  BIAS_STEP = 0.1,
  BIAS_MIN = 1
)

# Scoring
SCORING <- list(
  MULTIPLIER = 0.005
)
